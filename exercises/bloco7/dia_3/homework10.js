const assert = require('assert');

const Verify = (password) => {
  // password.length > 8;
  // password !== null;
  // password min 1 uppercase;
  // password min 1 lowercase;
  // password min 1 typeof number;
};

assert.strictEqual(typeof(Verify) === 'function', true);






